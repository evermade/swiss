A simple block listing block.
A repeater of blocks, where the total number of items is stored in a data attribute on the parent section element, and using CSS to select and make the columns based on that count. This way we can control the columns size and how the respond.