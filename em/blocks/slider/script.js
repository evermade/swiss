(function() {

	//create empty object in the global em var, dont forget to add the init call in the main.js!
	em.slider = {};

	//call any functions to be trigger on dom ready
	em.slider.init = function(){
		em.slider.setup();
	};

	em.slider.setup  = function(){
	};

})();